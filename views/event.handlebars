<section class="dashboard-container">
        
    <h4 class="d-flex justify-content-center">Add a blog post</h4>

    <form action="" class="addEvent container">
        <div class="row">
        <div class="row">
            <input class="form-control w-50 mx-auto mb-2 opacity-75" type="text" name="eventTitle" id="eventTitle" placeholder="Title">
        </div>
        <div class="row">
            <input class="form-control w-50 mx-auto mb-2 opacity-75" type="date" name="eventDate" id="eventDate">
        </div>
        <div class="row">
            <input class="form-control w-50 mx-auto mb-2 opacity-75" type="text" name="eventDescription" id="eventDescription" placeholder="Post contents">
        </div>
        <div class="row">
            <input class="btn btn-outline-light form-control w-50 mx-auto mb-3" type="submit" name="event-submit" id="eventSubmitButton">
        </div>
    </form>
<h4 class="events d-flex justify-content-center">Your posts</h4>
{{!-- going to need a db.query in my route --}}
    <section class="events-container w-50 mx-auto opacity-75">
            {{#each events}}
                   {{> event-details}}
            {{/each}}
    </section>

    {{!-- <section class="friends-container">
        <a href="friend/event/{{id}}">
        {{#each friend as |friend|}}
        {{> friend-details}}
        {{/each}}
        </a>
    </section> --}}
    </div>
</section>

<script src="/js/newEvent.js"></script>